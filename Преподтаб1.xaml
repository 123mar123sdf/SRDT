<Window x:Class="Расписание_занятий.Преподтаб1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Расписание_занятий"
        mc:Ignorable="d"
        Title="Преподаватели"    WindowStartupLocation="CenterScreen"  Height="532" Width="1372" >
    <Window.Background>
        <ImageBrush ImageSource="Иконки\ФФон.png" />
    </Window.Background>
    <Grid>
       

        <Frame Name="MainFrame"  NavigationUIVisibility="Hidden" ContentRendered="MainFrame_ContentRendered"  Navigated="MainFrame_Navigated"/>
        <Button  Style="{DynamicResource DefaultButtonStyle}" x:Name="Back" ToolTip="Вернуться назад" Click="Back_Click"
Margin="1290,0,0,436" BorderBrush="{x:Null}" Background="{x:Null}" RenderTransformOrigin="0.549,0.641">
            <Image Source="Иконки/Назад.png" Height="32" Width="32"></Image>
        </Button>
    </Grid>

</Window>
