<Page x:Class="Расписание_занятий.Добавитьзаданиевторник"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Расписание_занятий"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Добавитьзаданиевторник" Height="532" Width="1372">

    <Grid>

        <Button  Style="{DynamicResource DefaultButtonStyle}"   HorizontalAlignment="Left" VerticalAlignment="Top" Width="88" Margin="938,213,0,0" Click="Button_Click"  Height="67" Background="{x:Null}" BorderBrush="{x:Null}">
            <Image Source="Иконки/Сохранить.png" Height="32" Width="32"></Image>
            <Button.ToolTip>
                <ToolTip>
                    Сохранить
                </ToolTip>
            </Button.ToolTip>
        </Button>
        <Label x:Name="label_Copy2" Content="Комментарий к занятию:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="417,235,0,0" FontSize="12" FontFamily="Segoe UI" Width="177"/>
        <TextBox x:Name="text_Copy1" Text="{Binding Комментарий_преподавателя}"  HorizontalAlignment="Left" Height="26" Width="334" Margin="599,235,0,0" TextWrapping="Wrap" VerticalAlignment="Top"   FontFamily="Segoe UI" FontSize="12"/>

    </Grid>
</Page>
