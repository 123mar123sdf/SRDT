<Window x:Class="Расписание_занятий.О_Программе"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Расписание_занятий"
        mc:Ignorable="d"
        Title="Расписание занятий" WindowStyle="None"   WindowStartupLocation="CenterScreen"  Background="#FFE4CFBC" Height="544.737" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0*"/>
        </Grid.ColumnDefinitions>
        <TextBlock
            Text="Назначение информационной системы"
            Foreground="#FF000304" Margin="137,131,136,259" FontSize="30"  FontFamily="Segoe UI"    />
        <TextBlock
            Text="Информационная система позволяет"
            Foreground="#FF000304" Margin="82,226,56,147" FontSize="24"  FontFamily="Segoe UI"    />
        <TextBlock
            Text="обрабатывать информацию и создавать расписание занятий"
            Foreground="#FF000304" Margin="82,255,57,115" FontSize="24"  FontFamily="Segoe UI"    />
        <TextBlock
            Text="для учреждений дополнительного образования"
            Foreground="#FF000304" Margin="82,283,136,92" FontSize="24"  FontFamily="Segoe UI"    >

        </TextBlock>
        <Button Style="{DynamicResource zz}"   Content="Назад" HorizontalAlignment="Left" VerticalAlignment="Top" Width="152" Margin="320,427,0,0" Click="Button_Click"    Height="60"  FontFamily="Segoe UI" Background="{x:Null}" RenderTransformOrigin="0.5,0.5" />

    </Grid>
</Window>
